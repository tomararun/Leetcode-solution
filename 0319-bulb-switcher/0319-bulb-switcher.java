class Solution {

    // the numbers which have odd number of facters will remains on
    // and the numbers which have even number of facters will remains off

    // which numbers have odd number of factors
    // only perfect square number will have odd number of factors
    public int bulbSwitch(int n) {
        
        return (int)Math.sqrt(n);
    }
}